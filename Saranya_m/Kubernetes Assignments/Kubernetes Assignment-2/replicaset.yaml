apiVersion: apps/v1         
kind: ReplicaSet
metadata:
  name: replicademo-rs             
spec:
  replicas: 4               
  selector:
    matchLabels:
      app: web             
  template:                  
    metadata:
      labels:
        app: web           
    spec:
      containers:
      - name: nginx
        image: nginx:1.30 
        

