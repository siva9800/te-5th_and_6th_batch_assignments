apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    created-by: Gowthami
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
      - name: nginx
        image: nginx:1.25
        ports:
        - containerPort: 80